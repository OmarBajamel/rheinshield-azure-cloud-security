# Contributing

Use a focused branch, run `make validate`, keep all demonstration data synthetic, and include evidence for security-control changes. Never submit tenant IDs, subscription IDs, UPNs, real IP addresses, secrets, state files, or portal exports. Changes to detections require a malicious fixture, a benign fixture, MITRE mapping, and tuning notes.
