# Secure configuration standard

Secure defaults include encryption in transit, RBAC authorization, no public blob access, restricted network paths, structured logs without secrets, health probes, and explicit API versions. Terraform, policy, Sentinel, and application tests validate the public implementation. Drift and unsupported versions are tracked as risks.
